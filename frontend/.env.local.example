# this file is a template showing every environment variable the frontend needs
# copy this to .env.local and fill in your real values (never commit .env.local to git)

# the base url of the fastapi backend
# in development point this at localhost, in production point it at your render url
NEXT_PUBLIC_API_URL=http://localhost:8000
